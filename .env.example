# Leave empty or unset for same-origin deployments that proxy /api to the backend.
# Set this when the frontend calls a backend hosted on a different origin.
VITE_API_BASE_URL=https://api.yourdomain.com/api/v1
